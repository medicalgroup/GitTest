<view class='content'>
  <view class='both' wx:for="{{humanWealthDatas}}">
    <view class='humanWealth'>
      <view class='top'>
        <image src='{{item.image}}' style='width:150px;height:150px;'></image>
      </view>
      <view class='humanWealthBottom'>
        <text class='name'>{{item.name}}</text>
        <view>
          <text>{{item.detail}}</text>
        </view>
        <view class='humanWealthPrice'>
          <view>{{item.price}}元起</view>
          <view>{{item.type}}</view>
        </view>
      </view>
    </view>
  </view>

  <!-- <view class='all' wx:for="{{coverageDatas}}">
    <view class='coverage'>
      <view class='top'>
        <image src='{{item.image}}' style='width:150px;height:150px;'></image>
      </view>
      <view class='coverageBottom'>
        <text>{{item.name}}</text>
        <text>{{item.detail}}</text>
        <view class='coveragePrice'>
          <view>{{item.price}}元起</view>
          <view>{{item.type}}</view>
        </view>
      </view>
    </view>
  </view> -->
</view>
